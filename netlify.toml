[build]
  command = "npm run build"
  publish = "dist"

# SPA redirect — all routes serve index.html (required for hash-based routing)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[build.environment]
  NODE_VERSION = "20"
